services:
  db:
    image: postgres:17
    restart: always
    container_name: '${PREFIX}_db'
    ports:
      - '5434:5432'
    volumes:
      - ../db/postgres:/var/lib/postgresql/data
      - ../db/init.sh:/docker-entrypoint-initdb.d/init.sh
    env_file:
      - .env
    networks:
      - custom_network
    shm_size: '128mb'
    environment:
      POSTGRES_USER: '${POSTGRES_USER}'
      POSTGRES_PASSWORD: '${POSTGRES_PASSWORD}'
      POSTGRES_DB: '${POSTGRES_DB}'
      DB_USER: '${DB_USER}'
      DB_PASSWORD: '${DB_PASSWORD}'
      DB_NAME: '${DB_NAME}'

  # api-dev:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.dev
  #   image: ccbl-imagenes-back-dev
  #   container_name: '${PREFIX}_api_dev'
  #   restart: always
  #   ports:
  #     - '3000:3000'
  #   volumes:
  #     - ./:/app
  #     - /app/node_modules
  #   env_file:
  #     - .env
  #   networks:
  #     - custom_network
  #   depends_on:
  #     - db

networks:
  custom_network:
    driver: bridge
    ipam:
      config:
        - subnet: 192.168.2.0/24

volumes:
  postgres:
