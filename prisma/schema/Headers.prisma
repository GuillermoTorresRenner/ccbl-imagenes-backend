model Headers {
    id        String   @id @default(cuid())
    name      String
    imgId     String
    images    Image    @relation(fields: [imgId], references: [id])
    title     String
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}
